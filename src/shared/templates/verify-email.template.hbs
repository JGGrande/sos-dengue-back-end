<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verificação de E-mail</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    .titulo {
      font-family: 'Arial';
    }
    .sub-titulo {
      font-family: 'Arial';
    }
    .button-verificar {
      appearance: button;
      background-color: #405cf5;
      border-radius: 6px;
      box-shadow: rgba(50, 50, 93, .1) 0 0 0 1px inset,rgba(50, 50, 93, .1) 0 2px 5px 0,rgba(0, 0, 0, .07) 0 1px 1px 0;
      box-sizing: border-box;
      color: #fff;
      cursor: pointer;
      font-family: 'Arial';
      font-size: 100%;
      height: 44px;
      line-height: 1.15;
      margin: 12px 0 0;
      outline: none;
      overflow: hidden;
      padding: 1.5vh 0px;
      text-align: center;
      text-decoration: none;
      font-weight: bold;
      width: 100%;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="titulo">Verificação do e-mail</h1>

    <h2 class="sub-titulo">Olá, {{ name }}</h2>

    <h2 class="sub-titulo">Por favor clique no botão para verificar e-mail</h2>

    <a href="{{ urlToVerify }}" class="button-verificar">
      Verificar
    </a>
  </div>
</body>
</html>
